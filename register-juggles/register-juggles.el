











(char-after ?a)
(char-)
(number-)
(characterp ?)
(characterp (make-string  1 (+ ?a 1) ))

(char-after )


(progn

  (evil-show-registers)
  )





(--dotimes 10
  (evil-set-register (+ ?a it) (format "hello, mofo %s" it))
  )

(--doto )

(let ((res '()))
  (--dotimes 10
    (push it res)
    )
  res)
